spring:
  datasource:
    url: jdbc:mysql://localhost:3306/talki?serverTimezone=Asia/Seoul
    username: talki
    password: ${MYSQL_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
